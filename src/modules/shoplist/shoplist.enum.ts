import { PortionType as PrismaPortionType } from '@prisma/client';

export const PORTION_TYPE = PrismaPortionType;

export const ITEM_STATE = {
  IN_CART: 'IN_CART',
  REMOVED: 'REMOVED',
  IN_PANTRY: 'IN_PANTRY',
};
